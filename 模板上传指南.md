# Excel 模板下载功能 - 配置指南

## ✅ 已完成的功能

1. **UI 界面**：在上传页面顶部添加了醒目的模板下载区域
2. **下载按钮**：点击可下载并打开 Excel 模板文件
3. **样式美化**：使用渐变色背景，提升视觉效果
4. **错误处理**：完善的错误提示和引导

---

## 📋 配置步骤（必须完成）

### 第一步：准备模板文件

1. 准备你的 Excel 模板文件（.xlsx 格式）
2. 建议命名为：`industry_data_template.xlsx`
3. 确保模板包含所有必需的字段和格式说明

### 第二步：上传到云存储

#### 方式一：通过微信开发者工具上传（推荐）

1. 打开**微信开发者工具**
2. 点击顶部菜单 **"云开发"**
3. 选择 **"存储"** 标签
4. 点击 **"上传文件"** 按钮
5. 创建文件夹路径：`templates/`
6. 上传你的模板文件到 `templates/` 文件夹
7. 上传成功后，点击文件名，复制 **文件 ID**（格式类似：`cloud://env-xxx.xxxx-xxxx/templates/industry_data_template.xlsx`）

#### 方式二：通过云开发控制台上传

1. 登录 [微信云开发控制台](https://console.cloud.tencent.com/tcb)
2. 选择你的环境
3. 进入 **"存储"** → **"文件管理"**
4. 创建 `templates` 文件夹
5. 上传模板文件
6. 复制文件 ID

### 第三步：配置文件 ID

打开 `pages/upload/index.js` 文件，找到第 48 行左右：

```javascript
// 模板文件云存储路径（请替换为你上传后的实际路径）
templateFileID: 'cloud://your-env-id.xxxx-xxxx/templates/industry_data_template.xlsx'
```

**替换为你复制的实际文件 ID**，例如：

```javascript
templateFileID: 'cloud://prod-1a2b3c.7072-prod-1a2b3c-1234567/templates/industry_data_template.xlsx'
```

### 第四步：测试功能

1. 保存代码并重新编译
2. 在小程序中打开上传页面
3. 点击 **"下载 Excel 模板文件"** 按钮
4. 确认模板文件能正常打开

---

## 🎨 功能特性

### 1. 用户体验优化
- **醒目提示**：渐变色背景 + 提示文字，引导用户下载模板
- **一键下载**：点击按钮即可下载并打开模板
- **右上角菜单**：用户可以转发或保存到本地

### 2. 错误处理
- **未配置提示**：如果未配置 templateFileID，会弹窗提示配置步骤
- **下载失败提示**：网络异常时给出友好提示
- **打开失败提示**：提示用户安装 WPS 或 Office 应用

### 3. 兼容性
- 支持 iOS 和 Android
- 自动调用系统默认的 Excel 应用打开文件
- 支持 WPS、Microsoft Excel、腾讯文档等应用

---

## 📱 用户使用流程

1. 打开上传页面
2. 看到顶部的模板下载区域（紫色渐变背景）
3. 点击 **"下载 Excel 模板文件"** 按钮
4. 等待下载完成（显示 loading）
5. 自动打开模板文件
6. 用户可以：
   - 直接查看模板格式
   - 点击右上角菜单保存到本地
   - 转发给其他人

---

## 🔧 高级配置（可选）

### 支持多个模板文件

如果需要为不同产业提供不同的模板，可以修改代码：

```javascript
data: {
  templates: {
    '钢铁材料': 'cloud://xxx/templates/steel_template.xlsx',
    '铝材料': 'cloud://xxx/templates/aluminum_template.xlsx',
    // ... 其他产业
    'default': 'cloud://xxx/templates/industry_data_template.xlsx'
  }
},

downloadTemplate: function () {
  const category = this.data.selectedCategory || 'default';
  const templateFileID = this.data.templates[category] || this.data.templates['default'];
  
  // ... 下载逻辑
}
```

### 添加下载统计

在下载成功后记录统计数据：

```javascript
success: (res) => {
  // 记录下载次数
  wx.cloud.callFunction({
    name: 'logTemplateDownload',
    data: {
      timestamp: new Date(),
      userOpenid: getApp().globalData.openid
    }
  });
  
  // ... 打开文件
}
```

---

## ❓ 常见问题

### Q1: 点击按钮提示"模板未配置"？
**A**: 说明你还没有上传模板文件或没有配置 templateFileID，请按照上面的步骤完成配置。

### Q2: 下载后无法打开文件？
**A**: 请确保手机上安装了 WPS、Microsoft Excel 或其他支持 .xlsx 格式的应用。

### Q3: 如何更新模板文件？
**A**: 在云存储中删除旧文件，上传新文件，文件 ID 保持不变即可。

### Q4: 模板文件大小有限制吗？
**A**: 建议模板文件不超过 5MB，过大的文件下载速度会较慢。

---

## 📞 技术支持

如有问题，请检查：
1. 云存储中是否存在模板文件
2. 文件 ID 是否正确配置
3. 小程序是否有云存储访问权限
4. 网络连接是否正常

---

## 🎉 完成

配置完成后，用户就可以方便地下载模板文件了！
